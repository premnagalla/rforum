require 'rails_helper'

RSpec.describe Commentable do
  # including_class = Class.new do
  #   include ActiveModel::Validations
  #   include Commentable

  #   attr_accessor :user_id
  # end

  # context 'associations' do
  #   # it { expect(including_class).to belong_to(:user) }
  #   # it { expect(including_class).to have_many(:comments) }
  # end

  # context 'validations' do
  #   it { expect(including_class).to validate_presence_of :user_id }
  # end
end
